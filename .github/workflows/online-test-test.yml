
name: lz - online-test-test

on:
  push:
    paths:
      - 'Landingzone/online-test-test.json'

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  landingzone:
    uses: ./.github/workflows/template-Landingzone.yml
    with:
      managementGroupName : online
      managementGroupHierarchy: test
      location: FranceCentral
      parameterFile: ./Landingzone/online-test-test.json
    secrets: inherit
