
name: lz - online-whitelisting-test

on:
  push:
    paths:
      - 'Landingzone/online-whitelisting-test.json'

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  landingzone:
    uses: ./.github/workflows/template-Landingzone.yml
    with:
      managementGroupName : online
      managementGroupSuffix: prod
      location: FranceCentral
      parameterFile: ./Landingzone/online-whitelisting-test.json
    secrets: inherit
