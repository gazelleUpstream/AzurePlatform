
name: lz - platform-management-tst

on:
  push:
    paths:
      - 'Landingzone/platform-management-tst.json'

  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  landingzone:
    uses: ./.github/workflows/template-Landingzone.yml
    with:
      managementGroupName : platform
      managementGroupSuffix: tst
      location: replaceMe-lz-location
      parameterFile: ./Landingzone/platform-management-tst.json
    secrets: inherit
